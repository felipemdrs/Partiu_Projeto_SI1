# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()

# Travels
GET		/travels/					controllers.Application.travelList()
GET		/travels/in					controllers.Application.travelListIn()
GET		/travels/admin				controllers.Application.travelListAdmin()
GET		/travels/board				controllers.Application.travelBoard()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
